# LogiCore — план редизайна «2026» (референс: скриншоты Design)

> Этот файл — источник правды по редизайну. Любой исполнитель (человек или ИИ) продолжает
> работу отсюда: смотри статусы этапов, помечай сделанное `[x]`, добавляй заметки в «Журнал».
> ГЛАВНОЕ ПРАВИЛО: логика не трогается. Меняется только представление (JSX-разметка/CSS).
> Проект в ПРОДАКШЕНЕ: перед пушем `npx tsc --noEmit` в apps/api и apps/web, пуш сразу после этапа.

## Целевой дизайн-язык (из референса)
- Светлая основа: фон страницы #f4f5f7, карточки белые, радиус 20–24, мягкие тени.
- Тёмные акцент-блоки: карта, тикер, featured-элементы — глубокий графит #0b0d12.
- Шапка: светлая, логотип «Logi**Core**» (Core — синий #1677ff), навигация — ПИЛЮЛИ
  (активный пункт — чёрная пилюля с белым текстом), справа поиск/уведомления/тема/профиль.
- Тикер живых событий под шапкой: бегущая строка «LC-XXXX · событие» с цветными точками статусов.
- Хиро секций: огромный заголовок слева (34–44px, weight 800), крупные метрики справа
  (число + подпись + цветной суб-лейбл: «+5,4%», «по графику», «внимание ⚠»).
- Action-bar: белая пилюля-строка (контекст слева, предупреждения + действия справа,
  главная кнопка — синяя пилюля).
- Featured-карточка заявки: слева таймлайн рейса (погрузка → текущий статус с % → выгрузка,
  итоги: маршрут/стоимость), справа тёмная карта живого отслеживания + строка водителя
  (аватар-инициалы, госномер, кнопки чат/звонок).
- Таблица: табы-фильтры пилюлями со счётчиками, строки с прогресс-баром маршрута
  (цвет = статус: синий в пути / зелёный завершён / красный проблема), водитель с
  аватаром-инициалами, сумма справа жирная tabular-nums.
- Статус-пилюли: уже реализованы (components/ui/StatusPill.tsx) — переиспользовать.

## Стратегия «не сломать»
- Ant Design НЕ выпиливаем разом: он остаётся движком форм/модалок/таблиц.
  Визуально перекрываем собственным скином (класс lc-page + новые lc-* классы в
  apps/web/src/app/globals.css). Отказ от antd — постепенный, экран за экраном.
- Все текущие колонки таблиц сохраняются (референс — неполный, у нас данных больше:
  Организация, Заказчик, Перевозчик, Менеджер, Дата погрузки и т.д.).
- Роль-зависимые данные не светить: маржу показывать только там, где она уже показывается
  (реестр), НЕ добавлять в заявки без проверки роли.
- data-guide-атрибуты не удалять (ИИ-гид). popupClassName у меню навигации не терять.

## Этапы

### Этап 0. Инфраструктура скина — [x] (была сделана ранее)
lc-page, lc-title/eyebrow, lc-status, lc-metric, lc-card, lc-filterbar, компактная тема antd.

### Этап 1. Страница «Заявки» → референсный вид — [x] (сделано в этой сессии)
Файлы: apps/web/src/app/company/orders/page.tsx, apps/web/src/app/globals.css
- [x] Хиро: крупный заголовок «Заявки» + 3 крупные метрики справа (Всего / В пути / Ожидают,
      с суб-лейблами; «Проблемы» появляется красным при наличии).
- [x] Тикер живых событий под шапкой страницы (v1: собирается из загруженных заявок —
      номер + статус + маршрут; цветные точки по статусу). v2 см. Этап 5.
- [x] Action-bar: «База заявок · сегодня», предупреждение «N заявок ожидают назначения»,
      кнопка «Создать заявку» (lc-cta). Экспорт отчёта — НЕ добавлен (на этой странице нет
      бэкенд-экспорта; есть в Реестре. Решить: линк на реестр или новый эндпоинт).
- [x] Featured-карточка выбранной заявки над таблицей: клик по строке выбирает заявку;
      таймлайн (погрузка → текущий статус с прогрессом % → выгрузка), стоимость, водитель
      с аватаром и госномером, кнопка «Открыть заявку». v1 БЕЗ карты (см. Этап 5).
- [x] Таблица: колонка «Маршрут» получила прогресс-бар статуса; колонка «Водитель» —
      аватар-инициалы. ВСЕ остальные колонки сохранены как были.
- [x] Табы «Все заявки / Архив» — уже пилюли (Этап 0), счётчики на месте.

### Этап 2. Шапка платформы → светлая пилюльная (референс) — [x] (сделано в этой сессии)
Файлы: apps/web/src/app/company/layout.tsx, globals.css
- [x] Логотип «Logi**Core**» (Core синий), светлый фон, blur.
- [x] Навигация: пилюли (активная — чёрная). Реализовано CSS-скином поверх antd Menu
      (класс app-header-2026), выпадашки групп стали светлыми карточками.
- [x] Справа: профиль (имя + подпись роли/компании), аватар-инициалы.
- [ ] Поиск и колокольчик уведомлений в шапке — ЗАГЛУШКИ НЕ СТАВИЛИСЬ (нет функционала).
      Делать вместе с Этапом 6 (глобальный поиск) и Этапом 7 (центр уведомлений).
- [ ] Переключатель светлая/тёмная тема (☀/🌙 на референсе) — НЕ делан. Крупная задача:
      нужна тёмная палитра всего скина. Решение за владельцем.

### Этап 3. Дашборд → референсный вид — [ ] TODO
Файл: apps/web/src/app/company/page.tsx
- [ ] Хиро-метрики в стиле Этапа 1 (сейчас lc-metric карточки — привести к крупным цифрам).
- [ ] Тикер (переиспользовать компонент из Этапа 1 — вынести в components/ui/LiveTicker.tsx,
      сейчас он написан инлайн в orders/page.tsx — ВЫНЕСТИ при первом переиспользовании).
- [ ] Featured-блок «последняя активная заявка» (переиспользовать из Этапа 1).

### Этап 4. Остальные страницы под скин — [ ] TODO (по одной, аккуратно)
Порядок: Реестр заявок → Счета → Взаиморасчёты → Контрагенты → Адреса → Транспорт →
Сотрудники → Бухгалтерия (обзор) → Договоры → Склад → Зарплата/Моя зарплата.
Для каждой: lc-page обёртка + хиро-заголовок с метриками (если есть) + карточки lc-card +
таблицы уже стилизуются глобально. НЕ переписывать логику загрузки/форм.

### Этап 5. Карта в featured-карточке + живой тикер — [ ] TODO
- [ ] Мини-карта 2GIS в featured-карточке заявки (тёмная не обязательна; точки маршрута
      из routePoints[].location.latitude/longitude, линия между ними). Референс-компонент:
      components/ui/DgisTrackingMap.tsx (загрузчик mapgl уже есть — вынести loadMapgl в
      общий util, чтобы не дублировать третий раз).
- [ ] Тикер v2: реальные события — нужен лёгкий эндпоинт GET /company/orders/events
      (последние N записей OrderStatusHistory по заявкам компании: orderNumber, status,
      changedAt). Обновление раз в 60с. Бэкенд: apps/api/src/company/* по образцу getCompanyOrders.
- [ ] Прогресс «В движении · 52%» по-настоящему: сейчас % считается по позиции статуса
      в цепочке STATUS_CHAIN (грубая оценка). Точный % — из GPS (дистанция пройдена/всего),
      требует данных трекинга — отдельная задача.

### Этап 6. Глобальный поиск в шапке — [ ] TODO (функционал, не только UI)
Поиск по номеру заявки/контрагенту → выпадающие результаты → переход. Бэкенд-эндпоинт нужен.

### Этап 7. Центр уведомлений (колокольчик) — [ ] TODO (функционал)
Лента: смены статусов, запросы подтверждения завершения, новые тикеты (для админа).

### Этап 8. Тёмная тема платформы — [ ] TODO (решение владельца)
Референс имеет обе темы. Нужны CSS-переменные для всего lc-скина + переключатель в шапке
+ тёмный вариант antd (theme.darkAlgorithm). Большая задача, делать последней.

## Журнал
- 2026-07-05 (Claude): создан план; выполнены Этапы 1 и 2 (страница «Заявки» + шапка),
  детали в коммитах. Тикер v1 — из загруженных заявок, без нового бэкенда.
  Следующему исполнителю: начинать с Этапа 3 (дашборд), при этом СНАЧАЛА вынести
  LiveTicker и FeaturedOrderCard из orders/page.tsx в components/ui/ для переиспользования.
