/**
 * Требования к паролю — в одном месте.
 *
 * Восемь символов, а не шесть: в системе живут деньги, счета и документы,
 * и шестизначный пароль для неё слаб.
 *
 * ВАЖНО: правило действует только на создание и смену пароля. На вход его
 * не ставим — у сотрудников, заведённых раньше, пароли по шесть символов,
 * и требование длины при входе просто заперло бы их снаружи.
 */
export const MIN_PASSWORD_LENGTH = 8;

export const PASSWORD_TOO_SHORT = `Пароль должен быть не короче ${MIN_PASSWORD_LENGTH} символов`;
