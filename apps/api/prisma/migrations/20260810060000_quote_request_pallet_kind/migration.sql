-- Вид паллеты в запросе на расчёт.
--
-- Количество там уже было, а вида не было: «две паллеты» без вида не
-- говорят ничего о том, влезет ли груз в машину — европаллет и
-- американский отличаются на треть площади. Одного вида на запрос
-- достаточно: смешанный состав появляется уже в заявке, где под него есть
-- список.
ALTER TABLE "QuoteRequest" ADD COLUMN "palletKind" TEXT;
