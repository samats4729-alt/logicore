-- Окончательный отказ: заявку признали чужой.
--
-- Обычный отказ — замечание, после него документы прикладывают заново, и
-- это правильно: настоящая компания должна иметь возможность исправиться.
-- Но если фирму зарегистрировал не её владелец, исправлять нечего — он
-- вернётся с тем же БИН завтра. Отметка закрывает и повторную подачу, и
-- работу в кабинете.
ALTER TABLE "Company" ADD COLUMN "verificationBlockedAt" TIMESTAMP(3);
