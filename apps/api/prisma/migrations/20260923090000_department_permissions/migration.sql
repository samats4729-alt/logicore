-- Права отдела.
--
-- Раньше права были только личные: «открыть финотделу согласование счетов»
-- означало обойти галочками всех поимённо, а нового сотрудника вспомнить
-- через неделю — когда он скажет, что кнопки нет.
--
-- Пустой список по умолчанию: в день обновления ни один отдел ничего не
-- даёт, и у людей остаются ровно те права, что у них были.
ALTER TABLE "Department" ADD COLUMN "permissions" TEXT[] DEFAULT ARRAY[]::TEXT[];
