-- Привязка записи журнала к рейсу.
--
-- Нужна, чтобы карточка заявки показывала свою историю: кто вложил документ,
-- кто поменял груз, кто переназначил водителя. Раньше запись про документ
-- ссылалась на сам документ, и собрать ленту по рейсу было нечем.
--
-- Миграция только добавляет столбец и индекс, существующие записи не трогает.

-- AlterTable
ALTER TABLE "AuditLog" ADD COLUMN "orderId" TEXT;

-- CreateIndex
CREATE INDEX "AuditLog_orderId_createdAt_idx" ON "AuditLog" ("orderId", "createdAt");
