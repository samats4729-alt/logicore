-- Количество паллет в запросе на расчёт.
--
-- В грузоперевозках по Казахстану считают именно паллетами: «две паллеты
-- Астана — Алматы» — обычная формулировка запроса, а вес клиент часто
-- называет приблизительно. Поле переносится в заявку при согласовании,
-- поэтому единица та же, что и там.
--
-- Только добавление колонки: существующие запросы не трогаются, у них
-- останется пусто.
ALTER TABLE "QuoteRequest" ADD COLUMN "palletCount" INTEGER;
