-- Состав груза в заявке: типы паллет с количеством, способы погрузки и
-- виды упаковки. Раньше было одно число «количество палет» — оно остаётся
-- и хранит итог, чтобы карточка рейса, кабинет водителя и печатные формы
-- продолжали работать.
-- Миграция только добавляет колонки, существующие данные не трогает.

-- AlterTable
ALTER TABLE "Order" ADD COLUMN     "loadingTypes" TEXT[],
ADD COLUMN     "packagingTypes" TEXT[],
ADD COLUMN     "pallets" JSONB;

