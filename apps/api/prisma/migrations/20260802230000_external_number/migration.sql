-- Номер рейса в системе клиента и правило, как он попадает в счёт.
--
-- Логисты писали такие номера (ID, СТС) в поле «Номер ТТН» — приспособили
-- чужое поле, раз своего не было. Теперь у номера своё место, а у
-- контрагента — подпись, под которой он идёт в счёт. Пустая подпись
-- означает «в счёт не выводить»: у разных клиентов правила разные.
--
-- Только добавление колонок, существующие данные не трогаются.
ALTER TABLE "Order" ADD COLUMN "externalNumber" TEXT;
ALTER TABLE "Company" ADD COLUMN "externalNumberLabel" TEXT;
